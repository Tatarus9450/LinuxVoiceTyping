# ── Whisper.cpp (cloned by install.sh) ──
whisper.cpp/

# ── Python cache ──
__pycache__/
*.pyc
*.pyo

# ── Local config (has machine-specific paths) ──
config.env

# ── Runtime temp files ──
*.wav
*.log
*.pid
