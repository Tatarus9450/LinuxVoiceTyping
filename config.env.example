# ===== Voice Agent Config =====

# Whisper model path (will be updated by install.sh)
WHISPER_MODEL="./whisper.cpp/models/ggml-medium.bin"
# WHISPER_MODEL="./whisper.cpp/models/ggml-large-v3.bin"

# Recording settings
REC_SECONDS_DEFAULT=8
AUDIO_WAV="/tmp/voice_agent.wav"

# Whisper language: "th" = Thai, "en" = English, "auto" = auto-detect
WHISPER_LANG="th"

# Typing delay (ms per char-ish, only for xdotool fallback)
TYPE_DELAY=6

# CPU Threads for Whisper (Increase for speed, e.g. 8-12)
WHISPER_THREADS="10"

# Optional: ALSA device override (leave empty to use system default)
# Find devices with: arecord -L
ARECORD_DEVICE=""

# UI Settings
POPUP_ENABLED=true
