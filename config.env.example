# ===== Voice Agent Config =====

# Whisper model path (choose one)
# WHISPER_MODEL="/path/to/voice-agent/whisper.cpp/models/ggml-medium.bin"
WHISPER_MODEL="./whisper.cpp/models/ggml-medium.bin"

# Recording settings
REC_SECONDS_DEFAULT=8
AUDIO_WAV="/tmp/voice_agent.wav"

# Whisper language (Thai)
WHISPER_LANG="th"

# Typing delay (ms per char-ish)
# Typing delay (ms per char-ish)
TYPE_DELAY=6

# CPU Threads for Whisper (Increase for speed, e.g. 8-12)
WHISPER_THREADS="10"

# Optional: ALSA device override (leave empty to use default)
# Find devices with: arecord -L
ARECORD_DEVICE="" 

# UI Settings
POPUP_ENABLED=true
